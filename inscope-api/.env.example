NODE_ENV=development
PORT=8787

# Supabase → Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:inscopei1@db.mclmagrbffvurlldltnv.supabase.co:5432/postgres sslmode=require

# 允许的前端域名（CORS）
# 注意：不要带末尾斜杠 /
CORS_ORIGIN = https://www.inscopei1.com.au

# 下面两个先留空，等发验证码时再填
RESEND_API_KEY=
EMAIL_FROM="InScope <noreply@inscope.site>"

# 先放一个足够长的随机字符串（后面会用到）
JWT_SECRET=please_change_to_a_long_random_string_123456